<Window x:Class="SherafyVideoMaker.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Sherafy Video Maker" Height="720" Width="1080" MinHeight="640" MinWidth="980">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0 0 0 12" VerticalAlignment="Top">
            <TextBlock Text="Audio:" Width="60" VerticalAlignment="Center" />
            <TextBox Text="{Binding Settings.AudioPath, UpdateSourceTrigger=PropertyChanged}" Width="380" Margin="4 0" />
            <Button Content="Browse" Width="80" Margin="6 0" Click="BrowseAudio" />
            <TextBlock Text="SRT:" Width="60" Margin="24 0 0 0" VerticalAlignment="Center" />
            <TextBox Text="{Binding Settings.SrtPath, UpdateSourceTrigger=PropertyChanged}" Width="380" Margin="4 0" />
            <Button Content="Browse" Width="80" Margin="6 0" Click="BrowseSrt" />
        </StackPanel>

        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" Margin="0 0 0 12" VerticalAlignment="Top">
            <TextBlock Text="Clips Folder:" Width="100" VerticalAlignment="Center" />
            <TextBox Text="{Binding Settings.ClipsFolder, UpdateSourceTrigger=PropertyChanged}" Width="380" Margin="4 0" />
            <Button Content="Browse" Width="80" Margin="6 0" Click="BrowseClipsFolder" />
            <TextBlock Text="Clip URL:" Width="80" Margin="18 0 0 0" VerticalAlignment="Center" />
            <TextBox Text="{Binding Settings.ClipUrl, UpdateSourceTrigger=PropertyChanged}" Width="300" Margin="4 0" />
            <Button Content="Download" Width="100" Margin="6 0" Click="DownloadClip" />
            <TextBlock Text="Aspect:" Width="60" Margin="24 0 0 0" VerticalAlignment="Center" />
            <ComboBox ItemsSource="{Binding AspectOptions}" SelectedItem="{Binding Settings.AspectRatio}" Width="120" />
            <TextBlock Text="FPS:" Width="40" Margin="12 0 0 0" VerticalAlignment="Center" />
            <ComboBox ItemsSource="{Binding FpsOptions}" SelectedItem="{Binding Settings.Fps}" Width="80" />
            <CheckBox Content="Use GPU when available" IsChecked="{Binding Settings.UseGpuWhenAvailable}" Margin="12 0 0 0" VerticalAlignment="Center" />
            <Button Content="Load SRT" Width="100" Margin="20 0 0 0" Click="LoadSrt" />
            <Button Content="Validate" Width="100" Margin="12 0 0 0" Click="ValidateSegments" />
            <Button Content="Render" Width="100" Margin="12 0 0 0" Click="RenderVideo" />
        </StackPanel>

        <DataGrid Grid.Row="2" Grid.ColumnSpan="2" ItemsSource="{Binding Segments}" AutoGenerateColumns="False" Margin="0 0 0 12" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="#" Binding="{Binding Index}" IsReadOnly="True" Width="40" />
                <DataGridTextColumn Header="Start" Binding="{Binding Start}" Width="120" />
                <DataGridTextColumn Header="End" Binding="{Binding End}" Width="120" />
                <DataGridTextColumn Header="Duration" Binding="{Binding Duration}" IsReadOnly="True" Width="90" />
                <DataGridTextColumn Header="Text" Binding="{Binding Text}" Width="*" />
                <DataGridTextColumn Header="Assigned Clip" Binding="{Binding AssignedClip}" Width="120" />
                <DataGridTextColumn Header="Clip URL" Binding="{Binding ClipUrl}" Width="200" />
                <DataGridComboBoxColumn Header="Fit Mode"
                                          ItemsSource="{Binding DataContext.FitModeOptions, RelativeSource={RelativeSource AncestorType=Window}}"
                                          SelectedItemBinding="{Binding FitMode, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                          Width="120" />
                <DataGridTextColumn Header="Speed" Binding="{Binding Speed}" Width="80" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBox Grid.Row="3" Grid.ColumnSpan="2" Text="{Binding LogText}" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" Height="140" IsReadOnly="True" TextWrapping="Wrap" />
    </Grid>
</Window>
