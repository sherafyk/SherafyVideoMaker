# Auto SRT Pipeline (Windows)

Drag an audio file onto `transcribe_and_build.bat` to automatically generate:

- Raw JSON transcription
- Clean, clip-friendly SRT subtitles
- Stock video keyword prompts per subtitle block

## Requirements

- Windows 11
- Python 3.10+
- FFmpeg available in PATH (recommended)

## Setup

```bash
pip install -r requirements.txt
